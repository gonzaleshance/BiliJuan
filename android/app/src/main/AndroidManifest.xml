<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BiliJuan"
        android:usesCleartextTraffic="true">

        <!-- ── Entry Point ── -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ── Auth ── -->
        <activity android:name=".activities.LoginActivity"    android:exported="false"/>
        <activity android:name=".activities.RegisterActivity" android:exported="false"/>

        <!-- ── Admin ── -->
        <activity android:name=".activities.admin.AdminDashboardActivity" android:exported="false"/>

        <!-- ── Customer ── -->
        <activity android:name=".activities.customer.HomeActivity"          android:exported="false"/>
        <activity android:name=".activities.customer.ProductDetailActivity" android:exported="false"/>
        <activity android:name=".activities.customer.OrderSummaryActivity"  android:exported="false"/>
        <activity android:name=".activities.customer.OrderTrackingActivity" android:exported="false"/>
        <activity android:name=".activities.customer.MyOrdersActivity"      android:exported="false"/>
        <activity android:name=".activities.customer.AccountActivity"       android:exported="false"/>
        <activity android:name=".activities.customer.EditProfileActivity"   android:exported="false"/>
        <activity android:name=".activities.customer.SearchActivity"        android:exported="false"/>
        <activity android:name=".activities.customer.StoreDetailActivity"   android:exported="false"/>
        <activity android:name=".activities.customer.PinLocationActivity"   android:exported="false"/>
        <activity android:name=".activities.customer.CategoryActivity"         android:exported="false"/>
        <activity android:name=".activities.customer.CategoryFoodListActivity" android:exported="false"/>

        <!-- ── Seller ── -->
        <activity android:name=".activities.seller.SellerDashboardActivity"    android:exported="false"/>
        <activity android:name=".activities.seller.SellerOrdersActivity"       android:exported="false"/>
        <activity android:name=".activities.seller.AddProductActivity"         android:exported="false"/>
        <activity android:name=".activities.seller.SellerAccountActivity"      android:exported="false"/>
        <activity android:name=".activities.seller.EditSellerProfileActivity"  android:exported="false"/>
        <activity android:name=".activities.seller.SellerDeliveryMapActivity" android:exported="false"/>
        <activity android:name=".activities.seller.SellerPinLocationActivity"  android:exported="false"/>

        <!-- FCM Service -->
        <service
            android:name=".BiliJuanFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

    </application>
</manifest>